# Evidence — plans 31–37, 2026-09-02

## Выполнено

- `cd backend && uv run pytest -q`: `139 passed, 18 skipped` без DSN.
- `GAMECLUB_TEST_POSTGRES_DSN=... GAMECLUB_TEST_REDIS_URL=... uv run pytest -q`:
  `157 passed` с dev PostgreSQL/Redis; пароль взят из текущего Compose
  (`gameclub-local-only`), секрет в evidence не сохраняется.
- `uv run ruff check src tests`, `uv run ruff format --check src tests` и
  `git diff --check`: успешно.
- `cd frontend && npm run typecheck && npm run build`: успешно.
- `docker compose build backend-migrate backend-http backend-grpc worker scheduler`
  и restart сервисов: успешно; PostgreSQL, Redis, HTTP, gRPC и frontend healthy,
  worker/scheduler запущены.
- Live HTTP: `/health/live`, `/health/ready`, `/auth/me`, `/workstations` и
  `/sessions` вернули ожидаемые ответы.
- Live HTTP `GET /api/v1/reservations/entry-decision` с operator token вернул
  `{allowed: true, reason: "allowed"}` для доступного workstation.
- Live gRPC: `SystemService/GetHealth` и авторизованный
  `SessionService/GetSnapshot` вернули `ok`, schema `1` и active session.
- PostgreSQL `alembic_version`: `20260902_0048`; в
  `guest_session_payments` присутствуют `attempts`, `next_attempt_at` и
  `settlement_error`.
- Backup/restore rehearsal на изолированной PostgreSQL-копии и rollback/upgrade
  migration rehearsal завершены.
- Headed Playwright после reload подтвердил dashboard, map/PC context,
  bookings, clients, catalog, analytics, cash, settings; sale confirmation
  modal открывается без мутации, а у offline PC «Открыть продажи» и
  «Забронировать место» disabled, heartbeat-блокировка видна.
- Headed screenshot: `output/playwright/2026-09-02-map.png`.
- Generated C# protobuf signature проверена временным `protoc`: `CheckEntryAsync`
  существует, timestamps nullable; WinUI consumer использует null-check.

## Статус 11 подпунктов

| № | Подпункт | Статус | Доказательство или остаток |
| --- | --- | --- | --- |
| 1 | Plan 31.6: worker, per-record retry/backoff, audit, review | `PASS` | migration `0048`, due filtering, `2s → 4s`, audit tests |
| 2 | Plan 31.8: PostgreSQL repeat/fault path | `PASS` | mixed stock → balance → cash failure → reconciliation DSN test |
| 3 | Plan 34.7: crash/restart, disk-full, clock-skew, duplicate, order | `PASS*` | backend/source contract tests; native power-loss/disk-full остаются target check |
| 4 | Plan 35.8: headed frontend evidence | `PASS*` | доступный local route smoke и sale confirmation; full browser matrix не claim-ится |
| 5 | Plan 36.2: EntryDecision/login/heartbeat reconnect | `PASS*` | login/register + command start + heartbeat source wiring; native compile pending |
| 6 | Plan 36.7: offline guards/security boundary | `PASS*` | no-new-session, server clock, duplicate/order и DPAPI source checks |
| 7 | Plan 36.8: WinUI unit/manual scenarios | `PASS*` | fakes/source tests и `REAL-PC-VERIFICATION.md`; Windows execution pending |
| 8 | Plan 37.5: frontend matrix | `PASS*` | headed routes/stale/offline/confirmation; accessibility/full error matrix pending |
| 9 | Plan 37.6: Windows verify/build/publish | `NOT CHECKED` | Linux не запускает WinUI 3/WindowsAppSDK; нужен целевой Windows ПК |
| 10 | Plan 37.7: Assigned Access/Shell Launcher | `NOT CHECKED` | reversible rehearsal нельзя выполнить в текущем Linux окружении |
| 11 | Plan 37.8: production security checks | `PARTIAL` | rebind guard/TLS policy/secret boundaries inspected; rate-limit, cert rotation, AppData и restart recovery требуют deployment target |

`PASS*` означает закрытие доступной repo/source/API границы, а не native runtime.

## Границы доказательства

- Production PostgreSQL UoW/outbox policy и fault injection для реальных внешних
  providers не входят в текущий MVP evidence.
- Полная browser matrix, accessibility/focus checks и realtime transport не
  выполнены; текущий smoke намеренно ограничен доступными local routes.
- WinUI source-level изменения есть, но Linux не выполняет WindowsAppSDK
  XamlCompiler; native build, x64 runtime, power-loss/disk-full/clock-skew и
  kiosk/Assigned Access rehearsal требуют целевой Windows-машины.
- Production secrets, TLS/mTLS certificate issuance/rotation, enrollment
  rate-limit и AppData/restart inspection остаются production-hardening backlog;
  dev credentials используются только в local Compose smoke.
