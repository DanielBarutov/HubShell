<Window
    x:Class="GameClub.Client.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid
        x:Name="ContentRoot"
        Background="{ThemeResource PageBackgroundBrush}"
        KeyDown="RecordKeyActivity"
        PointerPressed="RecordPointerActivity">

        <!-- Technical fallback shown only before a portal snapshot is available. -->
        <Grid Visibility="{Binding IsSecuredContentVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid Padding="22,20,18,16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Spacing="3">
                        <TextBlock Text="HUBSHELL" Foreground="{ThemeResource AccentBrush}" FontSize="11" FontWeight="SemiBold" />
                        <TextBlock Text="Игровое место" Foreground="{ThemeResource PrimaryTextBrush}" FontSize="20" FontWeight="SemiBold" />
                    </StackPanel>
                    <Button Grid.Column="1" Click="HideToTray" Padding="10,7" Background="{ThemeResource CardBackgroundBrush}" Foreground="{ThemeResource SecondaryTextBrush}">
                        <TextBlock Text="Скрыть" FontSize="11" />
                    </Button>
                </Grid>
                <ScrollViewer Grid.Row="1" Margin="0,20,0,0" Padding="0,0,0,16">
                    <StackPanel Spacing="10">
                        <Border Background="{ThemeResource CardBackgroundBrush}" BorderBrush="{ThemeResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="14">
                            <StackPanel Spacing="8">
                                <Grid ColumnSpacing="8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Ellipse Width="8" Height="8" Fill="{Binding ConnectionColor}" VerticalAlignment="Center" />
                                    <StackPanel Grid.Column="1" Spacing="2">
                                        <TextBlock Text="Подключение" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="10" />
                                        <TextBlock Text="{Binding ConnectionMessage}" Foreground="{ThemeResource PrimaryTextBrush}" FontSize="13" />
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </Border>
                        <Border Background="{ThemeResource CardBackgroundBrush}" BorderBrush="{ThemeResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="14" Visibility="{Binding IsActiveSessionVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel Spacing="8">
                                <TextBlock Text="Текущая сессия" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="10" />
                                <TextBlock Text="{Binding ActiveTimeSummary}" Foreground="{ThemeResource AccentBrush}" FontSize="24" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding ActiveSessionDescription}" Foreground="{ThemeResource PrimaryTextBrush}" FontSize="12" TextWrapping="Wrap" />
                                <Button Click="StopCurrentSession" IsEnabled="{Binding CanStopActiveSession}" Padding="0,9" Background="{ThemeResource AccentBrush}" Foreground="{ThemeResource AccentTextBrush}">
                                    <TextBlock Text="Завершить сессию и перезапустить ПК" FontSize="11" FontWeight="SemiBold" />
                                </Button>
                            </StackPanel>
                        </Border>
                        <Button Click="ToggleTransferPanel" HorizontalAlignment="Stretch" Padding="12,9" Background="{ThemeResource CardBackgroundBrush}" Foreground="{ThemeResource PrimaryTextBrush}">
                            <TextBlock Text="{Binding TransferToggleLabel}" FontSize="11" />
                        </Button>
                        <Border Background="{ThemeResource CardBackgroundBrush}" BorderBrush="{ThemeResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="14" Visibility="{Binding IsTransferPanelVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel Spacing="8">
                                <TextBlock Text="Перенос сессии" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="10" />
                                <TextBlock Text="{Binding TransferMessage}" Visibility="{Binding IsTransferMessageVisible, Converter={StaticResource BooleanToVisibilityConverter}}" Foreground="{ThemeResource AccentBrush}" FontSize="11" TextWrapping="Wrap" />
                                <Button Click="DismissSessionNotification" Visibility="{Binding IsTransferMessageVisible, Converter={StaticResource BooleanToVisibilityConverter}}" HorizontalAlignment="Left" Padding="4,3" Background="Transparent">
                                    <TextBlock Text="Закрыть уведомление" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="10" />
                                </Button>
                                <TextBox Header="ID нового ПК" Text="{Binding TransferTargetWorkstationId, Mode=TwoWay}" PlaceholderText="Например, vip-02" />
                                <Button Click="CreateTransferOffer" IsEnabled="{Binding CanCreateTransferOffer}" Padding="0,8">
                                    <TextBlock Text="Подготовить перенос" FontSize="11" />
                                </Button>
                                <TextBox Header="ID предложения" Text="{Binding IncomingTransferOfferId, Mode=TwoWay}" PlaceholderText="Вставьте ID предложения" />
                                <TextBox Header="Код подтверждения" Text="{Binding IncomingTransferToken, Mode=TwoWay}" PlaceholderText="Одноразовый код" />
                                <Button Click="ConfirmTransfer" IsEnabled="{Binding CanConfirmTransfer}" Padding="0,8" Background="{ThemeResource AccentBrush}" Foreground="{ThemeResource AccentTextBrush}">
                                    <TextBlock Text="Подтвердить перенос" FontSize="11" FontWeight="SemiBold" />
                                </Button>
                            </StackPanel>
                        </Border>
                        <Button Click="RefreshConnection" HorizontalAlignment="Stretch" Padding="0,10" Background="{ThemeResource AccentBrush}" Foreground="{ThemeResource AccentTextBrush}">
                            <TextBlock Text="Проверить соединение" FontSize="11" FontWeight="SemiBold" />
                        </Button>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Grid>

        <!-- Fullscreen access gate. -->
        <Grid Visibility="{Binding IsAccessGateVisible, Converter={StaticResource BooleanToVisibilityConverter}}" Background="#E607090D">
            <Border MaxWidth="920" Margin="40" Padding="28" HorizontalAlignment="Center" VerticalAlignment="Center" Background="{ThemeResource CardBackgroundBrush}" BorderBrush="{ThemeResource BorderBrush}" BorderThickness="1" CornerRadius="18">
                <Grid RowSpacing="22">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal" Spacing="9">
                            <Border Width="28" Height="28" CornerRadius="8" Background="{ThemeResource AccentBrush}">
                                <TextBlock Text="H" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{ThemeResource AccentTextBrush}" FontSize="15" FontWeight="Bold" />
                            </Border>
                            <TextBlock Text="HUBSHELL" VerticalAlignment="Center" Foreground="{ThemeResource AccentBrush}" FontSize="13" FontWeight="Bold" CharacterSpacing="90" />
                        </StackPanel>
                    </Grid>

                    <Grid Grid.Row="1" ColumnSpacing="34">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.1*" />
                            <ColumnDefinition Width="0.9*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Spacing="0">
                            <Border HorizontalAlignment="Left" Padding="9,6" Background="#14B6F35A" BorderBrush="#45B6F35A" BorderThickness="1" CornerRadius="6">
                                <StackPanel Orientation="Horizontal" Spacing="7">
                                    <Ellipse Width="7" Height="7" Fill="{Binding ConnectionColor}" VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding ConnectionMessage}" Foreground="{ThemeResource AccentBrush}" FontSize="9" />
                                </StackPanel>
                            </Border>
                            <TextBlock Margin="0,22,0,0" Text="{Binding AccessTitle}" Foreground="{ThemeResource PrimaryTextBrush}" FontSize="30" FontWeight="SemiBold" TextWrapping="Wrap" />
                            <TextBlock Margin="0,10,0,0" Text="{Binding AccessSubtitle}" MaxWidth="340" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="12" TextWrapping="Wrap" />
                            <Border Margin="0,28,0,0" Padding="12" Background="{ThemeResource InnerCardBackgroundBrush}" BorderBrush="{ThemeResource BorderBrush}" BorderThickness="1" CornerRadius="10">
                                <Grid ColumnSpacing="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Border Width="44" Height="44" CornerRadius="9" Background="#1461D8FF">
                                        <TextBlock Text="▣" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{ThemeResource SignalBrush}" FontSize="24" />
                                    </Border>
                                    <StackPanel Grid.Column="1" Spacing="3">
                                        <TextBlock Text="ИГРОВОЕ МЕСТО" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="8" CharacterSpacing="60" />
                                        <TextBlock Text="{Binding CurrentWorkstationLabel}" Foreground="{ThemeResource PrimaryTextBrush}" FontSize="18" FontWeight="SemiBold" />
                                    </StackPanel>
                                    <Ellipse Grid.Column="2" Width="22" Height="22" Fill="{ThemeResource AccentBrush}" VerticalAlignment="Center" />
                                </Grid>
                            </Border>
                            <StackPanel Margin="0,21,0,0" Orientation="Horizontal" Spacing="8">
                                <TextBlock Text="◆" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="10" />
                                <TextBlock Text="Защищённый вход по паролю" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="10" />
                            </StackPanel>
                        </StackPanel>

                        <Border Grid.Column="1" Padding="22" Background="{ThemeResource InnerCardBackgroundBrush}" BorderBrush="{ThemeResource BorderBrush}" BorderThickness="1" CornerRadius="12">
                            <Grid>
                                <StackPanel Visibility="{Binding IsUserLoginVisible, Converter={StaticResource BooleanToVisibilityConverter}}" Spacing="12">
                                    <TextBlock Text="01 / 01" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="9" CharacterSpacing="70" />
                                    <TextBlock Text="Войти в аккаунт" Foreground="{ThemeResource PrimaryTextBrush}" FontSize="21" FontWeight="SemiBold" />
                                    <TextBlock Text="Ник или телефон и пароль" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="10" />
                                    <TextBox Margin="0,8,0,0" Header="Ник или телефон" TextChanged="PortalIdentifierChanged" PlaceholderText="Например, player_01" MaxLength="64" Background="{ThemeResource InputBackgroundBrush}" />
                                    <PasswordBox x:Name="PortalPasswordBox" Header="Пароль" PasswordChanged="PortalPasswordChanged" PlaceholderText="Оставьте пустым только после сброса пароля" MaxLength="128" Background="{ThemeResource InputBackgroundBrush}" />
                                    <Button Click="LoginPortal" IsEnabled="{Binding CanLoginPortal}" Margin="0,8,0,0" Padding="0,12" Background="{ThemeResource AccentBrush}" Foreground="{ThemeResource AccentTextBrush}">
                                        <TextBlock Text="Войти  →" FontSize="12" FontWeight="SemiBold" />
                                    </Button>
                                    <Button Click="OpenPortalRegistration" HorizontalAlignment="Stretch" Padding="0,10" Background="Transparent" Foreground="{ThemeResource SecondaryTextBrush}">
                                        <TextBlock Text="Создать аккаунт  ›" FontSize="11" />
                                    </Button>
                                </StackPanel>

                                <StackPanel Visibility="{Binding IsPortalRegistrationVisible, Converter={StaticResource BooleanToVisibilityConverter}}" Spacing="12">
                                    <TextBlock Text="НОВЫЙ АККАУНТ" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="9" CharacterSpacing="70" />
                                    <TextBlock Text="Создать аккаунт" Foreground="{ThemeResource PrimaryTextBrush}" FontSize="21" FontWeight="SemiBold" />
                                    <TextBlock Text="Ник, телефон и пароль" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="10" />
                                    <TextBox Margin="0,8,0,0" Header="Ник" TextChanged="PortalNicknameChanged" PlaceholderText="От 3 символов" MaxLength="64" Background="{ThemeResource InputBackgroundBrush}" />
                                    <TextBox Header="Телефон" TextChanged="PortalPhoneChanged" PlaceholderText="+7 (9__) ___-__-__" MaxLength="18" Background="{ThemeResource InputBackgroundBrush}" />
                                    <PasswordBox x:Name="PortalRegistrationPasswordBox" Header="Пароль" PasswordChanged="PortalRegistrationPasswordChanged" PlaceholderText="От 4 символов" MaxLength="128" Background="{ThemeResource InputBackgroundBrush}" />
                                    <Button Click="RegisterPortal" IsEnabled="{Binding CanRegisterPortal}" Margin="0,8,0,0" Padding="0,12" Background="{ThemeResource AccentBrush}" Foreground="{ThemeResource AccentTextBrush}">
                                        <TextBlock Text="Создать аккаунт" FontSize="12" FontWeight="SemiBold" />
                                    </Button>
                                    <Button Click="CancelPortalRegistration" HorizontalAlignment="Stretch" Padding="0,10" Background="Transparent" Foreground="{ThemeResource SecondaryTextBrush}">
                                        <TextBlock Text="Назад ко входу" FontSize="11" />
                                    </Button>
                                </StackPanel>

                                <StackPanel Visibility="{Binding IsManagerLoginVisible, Converter={StaticResource BooleanToVisibilityConverter}}" Spacing="12">
                                    <TextBlock Text="ОБСЛУЖИВАНИЕ" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="9" CharacterSpacing="70" />
                                    <TextBlock Text="Режим менеджера" Foreground="{ThemeResource PrimaryTextBrush}" FontSize="21" FontWeight="SemiBold" />
                                    <TextBlock Text="Системные действия доступны только менеджеру клуба." Foreground="{ThemeResource SecondaryTextBrush}" FontSize="10" TextWrapping="Wrap" />
                                    <PasswordBox x:Name="ManagerPasswordBox" Margin="0,8,0,0" Header="Пароль менеджера" PasswordChanged="ManagerPasswordChanged" PlaceholderText="Введите пароль" MaxLength="128" Background="{ThemeResource InputBackgroundBrush}" />
                                    <Button Click="EnterMaintenance" IsEnabled="{Binding CanEnterMaintenance}" Margin="0,8,0,0" Padding="0,12" Background="{ThemeResource AccentBrush}" Foreground="{ThemeResource AccentTextBrush}">
                                        <TextBlock Text="Открыть обслуживание" FontSize="12" FontWeight="SemiBold" />
                                    </Button>
                                    <Button Click="CancelManagerLogin" HorizontalAlignment="Stretch" Padding="0,10" Background="Transparent" Foreground="{ThemeResource SecondaryTextBrush}">
                                        <TextBlock Text="Назад ко входу пользователя" FontSize="11" />
                                    </Button>
                                </StackPanel>

                                <StackPanel Visibility="{Binding IsMaintenanceVisible, Converter={StaticResource BooleanToVisibilityConverter}}" Spacing="12">
                                    <TextBlock Text="ОБСЛУЖИВАНИЕ" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="9" CharacterSpacing="70" />
                                    <TextBlock Text="Режим менеджера активен" Foreground="{ThemeResource PrimaryTextBrush}" FontSize="21" FontWeight="SemiBold" TextWrapping="Wrap" />
                                    <Border Padding="11" Background="#16314C2C" BorderBrush="#3AB6F35A" BorderThickness="1" CornerRadius="8">
                                        <TextBlock Text="Доступ менеджера подтверждён. Рабочие действия клиента приостановлены." Foreground="{ThemeResource PrimaryTextBrush}" FontSize="10" TextWrapping="Wrap" />
                                    </Border>
                                    <Button Click="LockClient" Margin="0,8,0,0" Padding="0,12" Background="{ThemeResource AccentBrush}" Foreground="{ThemeResource AccentTextBrush}">
                                        <TextBlock Text="Закрыть обслуживание" FontSize="12" FontWeight="SemiBold" />
                                    </Button>
                                </StackPanel>

                                <StackPanel VerticalAlignment="Bottom" Margin="0,12,0,0" Spacing="5">
                                    <TextBlock Text="{Binding AccessMessage}" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="9" TextWrapping="Wrap" />
                                    <TextBlock Text="Данные передаются серверу клуба и не сохраняются на ПК." Foreground="{ThemeResource SecondaryTextBrush}" FontSize="9" TextWrapping="Wrap" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </Grid>

                    <Grid Grid.Row="2" Padding="0,12,0,0" BorderBrush="{ThemeResource BorderBrush}" BorderThickness="0,1,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal" Spacing="7">
                            <Ellipse Width="6" Height="6" Fill="{Binding ConnectionColor}" VerticalAlignment="Center" />
                            <TextBlock Text="Подключение установлено" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="9" />
                        </StackPanel>
                        <TextBlock Grid.Column="1" Text="{Binding CurrentWorkstationLabel}" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="9" />
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <!-- Authenticated right-side widget. -->
        <ScrollViewer
            Padding="12"
            Visibility="{Binding IsPortalContentVisible, Converter={StaticResource BooleanToVisibilityConverter}}"
            HorizontalScrollBarVisibility="Disabled"
            VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="9">
                <Grid Padding="4,2,2,4" ColumnSpacing="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" Spacing="7">
                        <Border Width="23" Height="23" CornerRadius="7" Background="{ThemeResource AccentBrush}">
                            <TextBlock Text="H" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{ThemeResource AccentTextBrush}" FontSize="13" FontWeight="Bold" />
                        </Border>
                        <TextBlock Text="HUBSHELL" VerticalAlignment="Center" Foreground="{ThemeResource AccentBrush}" FontSize="10" FontWeight="Bold" CharacterSpacing="80" />
                    </StackPanel>
                    <Button Grid.Column="1" Click="HideToTray" Padding="7,5" Background="Transparent" Foreground="{ThemeResource SecondaryTextBrush}">
                        <TextBlock Text="Скрыть" FontSize="10" />
                    </Button>
                </Grid>

                <Border Background="{ThemeResource CardBackgroundBrush}" BorderBrush="{ThemeResource BorderBrush}" BorderThickness="1" CornerRadius="11" Padding="13">
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Spacing="3">
                            <TextBlock Text="ВЫ ВОШЛИ КАК" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="8" CharacterSpacing="70" />
                            <TextBlock Text="{Binding PortalAccountSummary}" Foreground="{ThemeResource PrimaryTextBrush}" FontSize="17" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding PortalContactSummary}" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="9" />
                        </StackPanel>
                        <Button Grid.Column="1" Click="LockClient" Padding="8,6" Background="Transparent" Foreground="{ThemeResource SecondaryTextBrush}" VerticalAlignment="Top">
                            <TextBlock Text="Выйти" FontSize="9" />
                        </Button>
                    </Grid>
                </Border>

                <Border Background="#24331C" BorderBrush="{ThemeResource AccentBrush}" BorderThickness="1" CornerRadius="11" Padding="13" Visibility="{Binding IsPortalPasswordSetupVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel Spacing="9">
                        <TextBlock Text="Обновите пароль" Foreground="{ThemeResource PrimaryTextBrush}" FontSize="16" FontWeight="SemiBold" />
                        <TextBlock Text="Администратор сбросил старый пароль. Задайте новый, чтобы продолжить." Foreground="{ThemeResource SecondaryTextBrush}" FontSize="9" TextWrapping="Wrap" />
                        <PasswordBox x:Name="PortalPasswordSetupBox" Header="Новый пароль" PasswordChanged="PortalPasswordSetupChanged" PlaceholderText="От 4 символов" MaxLength="128" Background="{ThemeResource InputBackgroundBrush}" />
                        <PasswordBox x:Name="PortalPasswordSetupConfirmationBox" Header="Повторите пароль" PasswordChanged="PortalPasswordSetupConfirmationChanged" PlaceholderText="Введите пароль ещё раз" MaxLength="128" Background="{ThemeResource InputBackgroundBrush}" />
                        <Button Click="SetPortalPassword" IsEnabled="{Binding CanSetPortalPassword}" Padding="0,10" Background="{ThemeResource AccentBrush}" Foreground="{ThemeResource AccentTextBrush}">
                            <TextBlock Text="Сохранить новый пароль" FontSize="10" FontWeight="SemiBold" />
                        </Button>
                    </StackPanel>
                </Border>

                <Border Background="{ThemeResource CardBackgroundBrush}" BorderBrush="{ThemeResource BorderBrush}" BorderThickness="0,1,0,1" Padding="10,8" Visibility="{Binding IsPortalReadyVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Ellipse Width="6" Height="6" Fill="{Binding ConnectionColor}" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="1" Text="{Binding ConnectionMessage}" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="9" />
                        <StackPanel Grid.Column="2" HorizontalAlignment="Right" Spacing="2">
                            <TextBlock Text="{Binding CurrentWorkstationLabel}" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="8" />
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Background="{ThemeResource CardBackgroundBrush}" BorderBrush="{ThemeResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="10" Visibility="{Binding IsPortalReadyVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Grid ColumnSpacing="7">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="1.25*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Spacing="4">
                            <TextBlock Text="БАЛАНС" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="7" CharacterSpacing="60" />
                            <TextBlock Text="{Binding PortalBalanceAmountSummary}" Foreground="{ThemeResource PrimaryTextBrush}" FontSize="17" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding PortalBonusSummary}" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="8" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Spacing="4">
                            <TextBlock Text="ОСТАТОК ПО БАЛАНСУ" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="7" CharacterSpacing="45" />
                            <TextBlock Text="{Binding PortalAvailableTimeValue}" Foreground="{ThemeResource AccentBrush}" FontSize="14" FontWeight="SemiBold" />
                            <TextBlock Text="расчёт по текущей ставке" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="8" />
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Background="#122130" BorderBrush="#4D61D8FF" BorderThickness="1" CornerRadius="10" Padding="12" Visibility="{Binding IsActiveSessionVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel Spacing="10">
                        <Grid>
                            <TextBlock Text="●  СЕССИЯ АКТИВНА" Foreground="{ThemeResource SignalBrush}" FontSize="8" CharacterSpacing="45" />
                            <TextBlock Text="{Binding CurrentWorkstationLabel}" HorizontalAlignment="Right" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="8" />
                        </Grid>
                        <Grid ColumnSpacing="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Spacing="4">
                                <TextBlock Text="ТЕКУЩИЙ РЕЖИМ" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="8" CharacterSpacing="45" />
                                <TextBlock Text="{Binding CurrentSessionModeSummary}" Foreground="{ThemeResource PrimaryTextBrush}" FontSize="15" FontWeight="SemiBold" TextWrapping="Wrap" />
                                <TextBlock Text="{Binding CurrentSessionTariffSummary}" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="8" TextWrapping="Wrap" />
                            </StackPanel>
                            <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center" Spacing="3">
                                <TextBlock Text="ОСТАЛОСЬ" Foreground="{ThemeResource SignalBrush}" FontSize="8" HorizontalAlignment="Right" />
                                <TextBlock Text="{Binding ActiveTimeSummary}" Foreground="{ThemeResource PrimaryTextBrush}" FontSize="17" FontWeight="SemiBold" HorizontalAlignment="Right" TextWrapping="Wrap" />
                            </StackPanel>
                        </Grid>
                        <Grid Padding="0,9,0,0" BorderBrush="#2B61D8FF" BorderThickness="0,1,0,0">
                            <TextBlock Text="Время обновляется по данным сервера" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="8" />
                            <Button Click="StopCurrentSession" IsEnabled="{Binding CanStopActiveSession}" HorizontalAlignment="Right" Padding="7,3" Background="Transparent" Foreground="{ThemeResource AccentBrush}">
                                <TextBlock Text="Завершить" FontSize="8" />
                            </Button>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Background="#25351E" BorderBrush="#45B6F35A" BorderThickness="1" CornerRadius="9" Padding="10" Visibility="{Binding IsPortalNotificationVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding PortalNotification}" Foreground="{ThemeResource PrimaryTextBrush}" FontSize="9" TextWrapping="Wrap" />
                        <Button Grid.Column="1" Click="DismissSessionNotification" Padding="3,1" Background="Transparent" Foreground="{ThemeResource AccentBrush}">
                            <TextBlock Text="×" FontSize="14" />
                        </Button>
                    </Grid>
                </Border>

                <Border Background="{ThemeResource CardBackgroundBrush}" BorderBrush="{ThemeResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="11" Visibility="{Binding IsTariffsVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel Spacing="8">
                        <StackPanel Spacing="3">
                            <TextBlock Text="ДОСТУПНО В АККАУНТЕ" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="8" CharacterSpacing="60" />
                            <TextBlock Text="Тарифы времени" Foreground="{ThemeResource PrimaryTextBrush}" FontSize="13" FontWeight="SemiBold" />
                        </StackPanel>
                        <ItemsControl ItemsSource="{Binding PortalTariffs}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{ThemeResource InnerCardBackgroundBrush}" BorderBrush="{ThemeResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="9" Margin="0,2">
                                        <Grid ColumnSpacing="8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Spacing="2">
                                                <TextBlock Text="{Binding Name}" Foreground="{ThemeResource PrimaryTextBrush}" FontSize="10" FontWeight="SemiBold" />
                                                <StackPanel Orientation="Horizontal" Spacing="5">
                                                    <TextBlock Text="{Binding DurationSummary}" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="8" />
                                                    <TextBlock Text="·" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="8" />
                                                    <TextBlock Text="{Binding PriceSummary}" Foreground="{ThemeResource AccentBrush}" FontSize="8" />
                                                </StackPanel>
                                            </StackPanel>
                                            <Button Grid.Column="1" Tag="{Binding Id}" Click="PurchaseTariff" Padding="8,5" Background="{ThemeResource AccentBrush}" Foreground="{ThemeResource AccentTextBrush}">
                                                <TextBlock Text="Купить" FontSize="9" FontWeight="SemiBold" />
                                            </Button>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <Border Background="{ThemeResource CardBackgroundBrush}" BorderBrush="{ThemeResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="11" Visibility="{Binding IsPortalReadyVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel Spacing="7">
                        <TextBlock Text="Пакеты времени" Foreground="{ThemeResource PrimaryTextBrush}" FontSize="12" FontWeight="SemiBold" />
                        <ItemsControl ItemsSource="{Binding PortalEntitlementQueue}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="9" TextWrapping="Wrap" Margin="0,2" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <Button Click="ActivateFirstPortalEntitlement" IsEnabled="{Binding CanActivatePortalEntitlement}" Padding="8,6">
                            <TextBlock Text="Активировать сохранённый пакет" FontSize="9" />
                        </Button>
                    </StackPanel>
                </Border>

                <Button Click="ToggleTransferPanel" HorizontalAlignment="Stretch" Padding="10,8" Background="{ThemeResource CardBackgroundBrush}" Foreground="{ThemeResource SecondaryTextBrush}" Visibility="{Binding IsPortalReadyVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="{Binding TransferToggleLabel}" FontSize="9" />
                </Button>
                <Border Background="{ThemeResource CardBackgroundBrush}" BorderBrush="{ThemeResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="11" Visibility="{Binding IsTransferPanelVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Перенос сессии" Foreground="{ThemeResource PrimaryTextBrush}" FontSize="11" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding TransferMessage}" Visibility="{Binding IsTransferMessageVisible, Converter={StaticResource BooleanToVisibilityConverter}}" Foreground="{ThemeResource AccentBrush}" FontSize="9" TextWrapping="Wrap" />
                        <TextBox Header="ID нового ПК" Text="{Binding TransferTargetWorkstationId, Mode=TwoWay}" PlaceholderText="Например, vip-02" />
                        <Button Click="CreateTransferOffer" IsEnabled="{Binding CanCreateTransferOffer}" Padding="0,7"><TextBlock Text="Подготовить перенос" FontSize="9" /></Button>
                        <TextBox Header="ID предложения" Text="{Binding IncomingTransferOfferId, Mode=TwoWay}" />
                        <TextBox Header="Код подтверждения" Text="{Binding IncomingTransferToken, Mode=TwoWay}" />
                        <Button Click="ConfirmTransfer" IsEnabled="{Binding CanConfirmTransfer}" Padding="0,8" Background="{ThemeResource AccentBrush}" Foreground="{ThemeResource AccentTextBrush}"><TextBlock Text="Подтвердить перенос" FontSize="9" FontWeight="SemiBold" /></Button>
                    </StackPanel>
                </Border>

                <Grid Padding="4,8,4,2" Visibility="{Binding IsPortalReadyVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="Завершить сессию и перезапустить ПК" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="8" />
                    <TextBlock Grid.Column="1" Text="HUBSHELL" HorizontalAlignment="Right" Foreground="{ThemeResource SecondaryTextBrush}" FontSize="8" />
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
