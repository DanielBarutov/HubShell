# План 45. Гостевой запуск Steam

## Цель

Добавить отдельный от HubShell контур для разрешённого клубом запуска Steam под
гостевыми аккаунтами. Он не создаёт сессии HubShell, не меняет тарифы, баланс или
продажи и не получает доступ к основной базе HubShell.

## Границы

- `steam-guest` — самостоятельный .NET-сервис с собственной PostgreSQL-базой и
  внутренним HTTP API без web-интерфейса.
- `HubShellSteam.exe` запускается гостем вручную с рабочего стола. Он берёт один
  свободный аккаунт, запускает Steam и не показывает пароль.
- Агент раз в 30 секунд подтверждает отдельному сервису, что Steam на этом ПК
  ещё работает. Если подтверждения нет пять минут, сервис возвращает аккаунт
  в пул. HubShell в этой схеме не участвует.
- Хранилища не общие: сервис не читает таблицы HubShell, а HubShell не читает
  таблицы гостевых аккаунтов.

## Правила первой версии

1. Аккаунт может быть выдан только одной станции одновременно.
2. Повторный запуск на той же станции возвращает прежнюю активную аренду, а не
   занимает второй аккаунт.
3. После закрытия Steam агент сразу освобождает аккаунт. Если агент или ПК
   пропал, сервер освобождает его через пять минут без подтверждения.
4. Ошибка запуска Steam не освобождает аккаунт автоматически: до истечения
   пяти минут он остаётся занят; затем сервер возвращает его в пул.
5. Логин и пароль шифруются в базе отдельным ключом сервиса; пароль не выводится
   в HTTP-ответах оператора, журналах или интерфейсе.
6. Доступ агента ограничивается отдельным ключом конкретной станции. Управление
   аккаунтами и станциями требует другого ключа администратора.
7. Агент читает `.env` рядом с `HubShellSteam.exe`. Для закрытой локальной сети
   разрешён HTTP только при явном `HUBSHELL_STEAM_ALLOW_HTTP=true`.

## Фазы

### Фаза 0 — контракт и изоляция

- [x] Зафиксировать отдельную границу, состояние аккаунта, аренду и правило
  освобождения после пяти минут без подтверждения от агента.
- [x] Добавить отдельный PostgreSQL-контейнер и сервис на порту `8200`.

### Фаза 1 — выдача и освобождение

- [partial] Реализованы PostgreSQL-хранилище, шифрование и HTTP API; запуск с
  настоящей PostgreSQL ещё не выполнен.
- [partial] Реализован `HubShellSteam.exe`, запуск Steam, подтверждение аренды
  раз в 30 секунд и серверное освобождение через пять минут без подтверждения;
  запуск настоящего Steam на Windows ещё не проверен.
- [partial] Добавлены unit-тесты чтения `.env`, освобождения, подтверждения
  работающей аренды и ошибки запуска; нужны проверки повторного запроса,
  пятиминутного освобождения и конкурентной выдачи на настоящей PostgreSQL.
- [x] Добавлен Windows PowerShell-скрипт с меню для регистрации ПК, добавления
  Steam-аккаунтов, просмотра пула и проверки связи. Секреты вводятся скрыто.
  Генератор ключей использует API, совместимый с Windows PowerShell 5.1.

## Исправления после ручной проверки

- [x] Steam запускается обычным окном, без параметра тихого запуска.
- [x] Убрана связь с `HubShell.exe`: Steam-агент продлевает аренду сам, а
  отдельный сервис освобождает её через пять минут после потери подтверждений.

### Фаза 2 — Windows-проверка

- [ ] Собрать и проверить приложение на реальном Windows-ПК со Steam.
- [ ] Проверить на реальном Windows-ПК: закрытие Steam сразу освобождает
  аккаунт, а остановка `HubShellSteam.exe` оставляет его занятым ровно до пяти
  минут и затем возвращает в пул.
- [ ] Проверить перезапуск агента при занятой аренде и ручную блокировку аккаунта.
